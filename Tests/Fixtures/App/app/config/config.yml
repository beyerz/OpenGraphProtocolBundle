framework:
    secret:          fa32rf423fasdfawa
    router:          { resource: "%kernel.root_dir%/config/routing.yml" }
    form:            true
    csrf_protection: true
    templating:
      engines: ['twig']
    session:         ~
    default_locale:  en
    translator:      { fallback: en }
    profiler:        { only_exceptions: false }

twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"